function PageTitle({ title }) {
  return (
    <h1 className="text-[#01796F] font-bold text-8xl self-center mt-4 mb-4 fade-in-text indie-text">
      {title}
    </h1>
  );
}

export default PageTitle;
